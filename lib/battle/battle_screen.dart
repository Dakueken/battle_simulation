/*
Zeitleiste
ListView.builder(
        padding: const EdgeInsets.all(10),
        itemCount: entries.length,
        itemBuilder: (context, index) {
          return Container(
            height: 60,
            color: Colors.white,
            child: Row(
              children: [
                Icon(Icons.person),
                Text("Character ${entries[index]}"),
              ],
            ),
          );
        },
      ),
      */
